# Spotify Web API (Client Credentials Flow)
# Create an app at https://developer.spotify.com/dashboard to get these values.
# Server-side only, never exposed to the browser, never commit real values.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
